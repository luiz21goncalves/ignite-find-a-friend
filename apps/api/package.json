{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run services:up && node --import tsx --env-file .env.development src/server.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "services:up": "docker compose up -d",
    "services:down": "docker compose down",
    "services:stop": "docker compose stop"
  },
  "dependencies": {
    "@fastify/cors": "9.0.1",
    "fastify": "4.26.2",
    "zod": "3.22.4"
  },
  "devDependencies": {
    "@ignite-find-a-friend/eslint-config": "workspace:*",
    "@ignite-find-a-friend/typescript-config": "workspace:*",
    "@types/node": "20.10.6",
    "eslint": "8.56.0",
    "tsx": "4.7.1",
    "typescript": "5.3.3"
  }
}
